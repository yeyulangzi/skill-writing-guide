# 高质量 Skill 编写规范指南

> 基于对全部 50+ 个已安装 Skill 的系统扫描与标杆识别，抽象出可复用的产品模式与技术模式。
> 每次创建或修改 Skill 时必须参考本文档，尽量避坑。

---

## 一、Skill 目录结构

```
my-skill/
├── SKILL.md          # ✅ 必需 — 技能主体（YAML frontmatter + Markdown 正文）
├── manifest.json     # 推荐 — 包清单（版本、来源追溯、市场分发）
├── examples.md       # 强烈推荐 — 重型示例参考（100+ 行时独立存放）
├── reference.md      # 可选 — API 文档 / 语法参考（重型资料）
├── templates/        # 产出物模板
├── scripts/          # 可执行脚本（确定性任务，执行时不加载进上下文）
└── assets/           # 静态资源（图标、字体、图片）
```

**拆分原则**：仅当内容超过 100 行或需要复用时拆出独立文件。宁缺毋滥。

---

## 二、渐进式加载（三级）

| 层级 | 内容 | 加载时机 | 上限 |
|------|------|----------|------|
| **L1 元数据** | name + description | 始终在上下文 | < 100 词 |
| **L2 SKILL.md 正文** | 技能主体指令 | 触发时加载 | < 500 行 |
| **L3 捆绑资源** | scripts / references / assets | 按需加载 | 无限制 |

---

## 三、产品视角：以用户结果为中心的 3 个评价维度

> 产品视角不关心 Skill "写得好不好"，而是关心 **Agent 用了这个 Skill 之后，执行结果好不好、过程顺不顺、复杂流程能不能走到终点**。

---

### 维度一：需求满足度 —— 执行结果对不对

**核心问题：Skill 触发后，Agent 产出的东西是不是用户真正想要的？**

| 评价指标 | 追问 | 标杆案例 |
|---------|------|---------|
| **任务完成率** | 不加 Skill 时 Agent 能完成吗？加了之后提升多少？ | TDD: baseline 阶段 Agent 直接写代码跳测试 → 加 Skill 后完成率从 ~30% → ~90% |
| **输出质量** | 产出物是否满足隐含的质量标准？不只是"做完了"而是"做好了" | systematic-debugging: 不加 Skill → 打补丁掩盖症状；加 Skill → 找到根因再修复 |
| **不误触发** | 不该用的时候会不会乱入？ | verification-before-completion: 只在"即将声称完成"时触发，不会干扰正常开发 |
| **覆盖真实痛点** | 解决的问题是 Agent 真的会犯的错，还是作者想象的？ | 借口表每行都来自 baseline 实测数据，不是凭空编造 |

**反例**：Skill 写得很漂亮，但 baseline 和加 Skill 的结果没区别 → 这个 Skill 没满足任何需求，白写了。

---

### 维度二：使用体验 —— 执行过程顺不顺

**核心问题：Agent 在遵循这个 Skill 的过程中，是顺畅的还是痛苦的？**

| 评价指标 | 追问 | 标杆案例 |
|---------|------|---------|
| **触发精准度** | 该触发时 100% 触发？描述的关键词覆盖了 Agent 的实际用语吗？ | dispatching-parallel-agents: description 覆盖 "2+ independent tasks"、"parallel"、"no shared state" 多个入口 |
| **认知负荷** | Agent 读 Skill 时能快速定位关键指令，还是被淹没在文字里？ | verification-before-completion: 139 行，Agent 5 秒扫完就知道该做什么 |
| **纠错成本** | Agent 走偏了，Skill 能不能快速拉回来？还是会在错误方向上越走越远？ | systematic-debugging: `≥3 次失败修复 = 质疑架构` 闸门，防止无限循环 |
| **自给自足** | Agent 是否需要用户介入才能完成？好 Skill 让 Agent 自己就能走完全程 | subagent-driven-development: 状态矩阵覆盖 DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED 四种情况，每种都有下一步动作 |
| **无歧义** | 指令是否明确到不会产生两种理解？ | TDD 的 Iron Law "NO PRODUCTION CODE WITHOUT A FAILING TEST FIRST" — 没有任何解释空间 |

**反例**：Skill 正文 500 行，Agent 扫了一遍没找到重点，凭感觉执行 → 体验极差，等于没加载。

---

### 维度三：流程引导充分性 —— 多步骤能不能走到终点

**核心问题：复杂流程（3 步以上）中，Agent 会不会中途迷失、跳步、或提前结束？**

| 评价指标 | 追问 | 标杆案例 |
|---------|------|---------|
| **步骤可感知** | Agent 是否始终知道"我在第几步，下一步是什么"？ | brainstorming: 9 步编号清单，每步有明确的完成标志 |
| **关键节点有闸门** | 不可跳过的检查点是否硬编码进流程？ | brainstorming: `<HARD-GATE>` 标签在创意发散结束后强制刹车，禁止直接开始写代码 |
| **异常路径有预案** | 不只是描述 happy path，出错/卡住/不确定时怎么办？ | subagent-driven-development: 四种 implementer 状态各有处理分支，不是只有"成功"一种 |
| **完成标准客观** | "做完了"不由 Agent 主观判断，而是有外部可验证的标准 | verification-before-completion: Gate 函数要求"提供新鲜验证证据"，不是"我觉得没问题了" |
| **不会提前终止** | 流程中是否有机制防止 Agent 在中间步骤就声称完成？ | writing-plans: Self-Review 清单强制在声称完成前逐项自查 |
| **不会无限循环** | 流程中是否有退出条件，防止 Agent 在某个步骤反复转圈？ | systematic-debugging: `≥3 次失败 = 质疑架构` 强制跳出调试循环 |

**反例**：Skill 描述了"应该做 A、B、C"，但 Agent 做到 B 时卡住了，Skill 没说卡住怎么办 → Agent 自己发挥，流程崩了。

---

### 三维度与 Skill 类型的侧重

| Skill 类型 | 需求满足权重 | 体验权重 | 流程引导权重 |
|-----------|:----------:|:------:|:----------:|
| **纪律规则**（TDD, debug, verify） | ★★★★★ | ★★★★ | ★★ |
| **技巧模式**（parallel agents） | ★★★★ | ★★★★★ | ★★★ |
| **工作流编排**（brainstorm, subagent, plans） | ★★★ | ★★★ | ★★★★★ |

> 纪律类 Skill 的核心价值是"产出正确"；技巧类 Skill 的核心价值是"用着顺手"；编排类 Skill 的核心价值是"走完全程不迷路"。

---

### 产品视角的底层：可发现性 & 可维护性（基础设施）

以上三维度是"执行层"的评价。但还有两个"基础设施层"的前提条件，做不到的话三维度无从谈起：

- **可发现性（Discoverability）**：description 必须以 `Use when...` 开头，只写触发条件，绝不泄露工作流。散布关键词覆盖 Agent 的实际用语。
- **可维护性（Maintainability）**：manifest.json 追溯版本来源；动名词命名（`writing-skills`）；独立文件拆分重型参考。

---

## 四、技术视角：好 Skill 的 5 个工程约束

### 1. Token 预算意识

- 频繁加载的 Skill：< 200 词
- 通用 Skill：< 500 行
- 用交叉引用替代重复内容
- 命令行细节不写进正文：`Run --help for details`

### 2. 防取巧设计（Anti-Rationalization）

Agent 在压力下（时间紧、沉没成本高）会找一切漏洞。好 Skill 需要：

- **Iron Law** — 一行大写铁律，不给解释空间
- **借口-现实对照表** — 预判 Agent 的每一个借口并提前驳回
- **Red Flags 自查清单** — "如果你在想 X → STOP"
- **"违反字面即违反精神"** — 封堵"我是在遵守精神"的借口类别

### 3. 交叉引用不粗暴加载

```markdown
✅ **REQUIRED BACKGROUND:** You MUST understand superpowers:test-driven-development
❌ @skills/testing/test-driven-development/SKILL.md  # 强制加载 200K+ token
```

### 4. 计算卸载

`scripts/` 目录的精妙之处：脚本执行时不加载到上下文，只拿 stdout。让上下文只承载判断和决策。

### 5. 流程图仅用于非显然决策

- ✅ 用流程图：多分支决策、循环流程（容易中途停止的场景）
- ❌ 不用流程图：参考材料 → 用表格；线性步骤 → 用编号列表

---

## 五、标杆 Skill 速查

| Skill | 类型 | 核心亮点 |
|-------|------|----------|
| **test-driven-development** | 纪律规则 | Iron Law + 借口表 + Red Flags + Good/Bad 对比 + 验证清单 |
| **systematic-debugging** | 纪律规则 | 四阶段流程 + `≥3 次失败 = 质疑架构` 闸门 + 交叉引用独立技术文件 |
| **verification-before-completion** | 纪律规则 | 极致 token 效率（139 行） + Gate 函数 + 失败矩阵 |
| **dispatching-parallel-agents** | 技巧模式 | 流程图决策树 + Agent 指令模板 + Common Mistakes ✅/❌ |
| **subagent-driven-development** | 工作流编排 | 双流程图 + 模型选择策略 + 状态矩阵 + 集成地图 |
| **brainstorming** | 工作流编排 | `<HARD-GATE>` 闸门 + 9 步清单 + 反模式显式命名 |
| **writing-plans** | 工作流编排 | Scope 检查 + "No Placeholders" 反模式 + Self-Review |
| **writing-skills** | 元技能 | TDD for Docs 方法论 + CSO 优化（含 A/B 测试证据）+ 防取巧工程 |
| **huashu-design** | 领域知识+工作流编排 | 核心资产协议（Logo>产品图>色值>字体优先级金字塔）+ 设计方向顾问 Fallback（5流派×20哲学）+ 规则附真实失败故事+成本对比 + Starter Components硬性绑定 + 反AI slop逻辑链论证 |

---

## 六、13 个被验证好用的写作套路

> 不是什么理论框架，就是从 8 个标杆 Skill 里反复出现的设计手法中提炼出来的"常见配方"。
> 写新 Skill 时对照着看——缺了哪个，对应维度就可能是短板。
> 🎯 = 需求满足度 &nbsp;&nbsp; 🧭 = 使用体验 &nbsp;&nbsp; 🔀 = 流程引导充分性

---

### 套路 1 — 第一行喊死规矩 🎯🧭

Skill 开头用一行大写喊出底线，不给 AI 任何狡辩空间。

```
TDD:  NO PRODUCTION CODE WITHOUT A FAILING TEST FIRST
Debug: NO FIXES WITHOUT ROOT CAUSE INVESTIGATION FIRST
Verify: NO COMPLETION CLAIMS WITHOUT FRESH VERIFICATION EVIDENCE
```

效果：AI 读完这行就知道"这事没商量"，不会试图绕过去。

---

### 套路 2 — 预判 AI 会偷什么懒，提前怼回去 🎯

把 AI 在压力测试里真实说过的借口列出来，旁边直接写真相。

```markdown
| AI 说的借口 | 实际情况 |
|"太简单不用写测试" | 简单代码也会坏，写个测试只花 30 秒 |
|"我先手动测过了" | 临时测 ≠ 有记录，下次改代码不能重跑 |
```

**关键**：每条借口必须来自 baseline 实测时 AI 真实说过的话，不是凭空想象。凭空编的借口 AI 不会对号入座。

---

### 套路 3 — 告诉 AI"如果你脑子冒出这句话，立刻停" 🧭

用 AI 自己容易产生的内心独白做触发词，一旦出现就强制刹车。

```markdown
## ⛔ 出现以下想法，立刻停手重来
- "代码我都写差不多了，补个测试就行"
- "我已经手动测过了，没问题"
- "这个特殊情况不适用这个规则因为…"
→ 以上任何一个 = 删掉代码，从测试重新开始
```

---

### 套路 4 — 不只写"什么时候用"，还要写"什么时候别用" 🎯🧭

只写触发条件不写排除条件 = Skill 会被滥用到不该用的场景。AI 不会自己判断边界。

```markdown
## When to Use
- 要写新功能或修 bug 之前

## When NOT to Use
- 只是改注释或文案
- 在 REPL 里随手试东西
- 修一个已经有测试覆盖的简单 bug
```

---

### 套路 5 — 文字说不清的岔路口才画图 🔀

线性步骤画流程图 = 比编号列表更难读。只有多分支、容易走错的决策才值得用图。

✅ 用图："先诊断还是先修？看这 3 个条件"  
❌ 不用图："第一步装依赖，第二步改代码，第三步跑测试"

---

### 套路 6 — 长 Skill 末尾放张小抄 🧭

AI 第二次触发同一个 Skill 时不会从头读到尾。末尾放一张速查表，扫一眼就够。

```markdown
## Quick Reference
| 场景 | 动作 |
|------|------|
| 写新功能 | TDD → test → code → refactor |
| 修 bug | Debug → root cause → fix → verify |
| 声称完成 | Verify → 新鲜证据 → 才能说完成 |
```

---

### 套路 7 — 用数字说话，不讲道理 🎯

"这样做更好" → AI 不信。"不加这个 Skill 完成率 30%，加了 90%" → AI 信了。

数字比任何修辞都有说服力。每个纪律类 Skill 尽量在 Real-World Impact 里放一组 before/after 数据。

---

### 套路 8 — 引用其他 Skill 但不强制加载全文 🧭

提到另一个 Skill 的时候，用语义引用而不是文件路径引用，避免把几百行内容强行拉进上下文。

```markdown
✅ **你需要先理解：** superpowers:test-driven-development
❌ @skills/testing/test-driven-development/SKILL.md
```

---

### 套路 9 — 不让 AI 自己判断"做完了没" 🔀🎯

"做完了"三个字太主观。给一张外部清单，逐项打勾才算数，AI 不能自己宣布胜利。

```markdown
## 完成前必须全部打勾
- [ ] 所有测试通过（终端输出为证）
- [ ] 没有跳过任何步骤
- [ ] 没有用"太简单""特殊情况"做借口
- [ ] 上述证据新鲜，不是上一次跑的
```

---

### 套路 10 — 画一张"换乘地图" 🔀

告诉 AI 进这个 Skill 之前要先加载哪个，做完之后要接哪个。像地铁换乘图，AI 不会走丢。

```markdown
## 工作流衔接
**进来之前先加载：**
- superpowers:using-git-worktrees — 先建独立工作区
**做完之后接：**
- superpowers:finishing-a-development-branch — 决定合并还是提 PR
```

---

### 套路 11 — 不只说"别做X"，把逻辑链讲完 🎯

只写"禁止用紫渐变"是审美洁癖，Agent 会当耳旁风。把"为什么禁止"的逻辑链讲完——从用户利益受损的角度论证，规则就从"设计师的个人品味"升级为"对用户负责的商业逻辑"。

huashu-design 的反 AI slop 论证是典范：

> 1. 用户请你做设计，是要**他的品牌被认出来**
> 2. AI 默认产出 = 训练语料的平均 = 所有品牌混合 = **没有任何品牌被认出来**
> 3. 所以 AI 默认产出 = 帮用户把品牌稀释成"又一个 AI 做的页面"
> 4. 反 slop 不是审美洁癖，是**替用户保护品牌识别度**

写法模板：

```markdown
## 为什么禁止 X？
X 本身不一定丑，但它是 AI 训练语料里的"视觉最大公约数"——
不携带任何品牌信息。用 X = 帮用户把品牌稀释成"又一个 AI 做的页面"。

**合法破例**：品牌 spec 里明写了用 X → 此时 X 不再是 slop，是品牌签名。
```

**关键**：给规则一个"合法破例"的出口。一刀切禁止会让 Agent 在合理场景下也不敢用；有判断框架的规则才能被准确执行。

---

### 套路 12 — 需求模糊时不追问，给结构化选项 🧭🔀

用户说"做个好看的"、"帮我设计"、"不知道要什么风格"——Agent 的本能是追问 10 个问题。但追问对用户是认知负担。更好的做法：**用预置的领域知识库，生成 3 个差异化选项让用户选**。

huashu-design 的做法：预置 5 大流派 × 20 种设计哲学的知识库，需求模糊时从不同流派推荐 3 个方向，每个方向附设计师/机构名 + 50-100 字解释 + 视觉特征 + 气质关键词。用户只需要"选一个"，不需要"想一个"。

写法模板：

```markdown
## Fallback 模式（需求模糊时触发）
当用户需求模糊到无法直接执行时，不要追问，而是：
1. 从预置知识库（见 references/xxx.md）推荐 3 个差异化方向
2. 每个方向给出：适用场景 + 标志性特征 + 为什么适合用户
3. 用户选定后，回到主干流程

**不触发 Fallback**：用户已给明确参考（截图/规范/具体风格描述）→ 直接走主干。
```

**关键**：Fallback 不是"放弃思考"，而是"替用户做了预思考"。知识库的质量决定 Fallback 的质量。

---

### 套路 13 — 规则附上真实失败故事 + 成本对比 🎯

"这样做更好" → Agent 不信。"上次没做这个，踩了坑，返工 2 小时" → Agent 信了。

huashu-design 的每条硬规则背后都附着一个真实踩过的坑和成本数字：

```markdown
## 核心原则 #0 · 事实验证先于假设
**反例（2026-04-20 真实踩过的坑）**：
- 用户："给大疆 Pocket 4 做发布动画"
- 我：凭记忆说"Pocket 4 还没发布，我们做概念 demo"
- 真相：Pocket 4 已在 4 天前发布
- 后果：基于错误假设返工 1-2 小时
- **成本对比：WebSearch 10 秒 << 返工 2 小时**
```

写法要素：
- **具体场景**（谁、什么任务、什么时间）
- **错误行为**（Agent 当时做了什么）
- **真相/根因**（什么是正确的）
- **后果量化**（浪费了多少时间/产生了什么坏结果）
- **成本对比**（遵守规则的成本 vs 不遵守的代价，用数字）

**关键**：故事必须真实，不能编造。编的故事 Agent 不会对号入座。日期和产品名越具体，说服力越强。

---

## 七、11 个工程实现技巧

> 和第六章的"写作套路"不同，这章说的是怎么让 Skill 在工程层面更省资源、更不容易被 AI 钻空子。

---

### 技巧 1 — 别客气，把 AI 当对手来写

写 Skill 不是"礼貌地给建议"，而是预判 AI 会在什么地方偷懒、找借口、走捷径，然后提前封死。

怎么做：
- 在 Skill 里写"违反字面就是违反精神"——堵死"我在遵守精神"这种借口
- 借口表里的每一句，必须是 baseline 测试时 AI 真实说过的原话
- Red Flags 用 AI 最可能冒出来的内心独白当触发词（"代码我都写差不多了，补个测试就行"）

---

### 技巧 2 — 字越少越好，废话全删

Skill 的每一个字都在消耗上下文预算。好 Skill 像电报，不像小说。

| 标杆 Skill | 行数 | 词数 |
|-----------|------|------|
| verification-before-completion | 139 | 668 |
| dispatching-parallel-agents | 182 | 923 |
| writing-plans | 152 | 914 |

**经验值**：超过 300 行就该想是不是要拆文件。频繁触发的 Skill 控制在 200 词以内。

---

### 技巧 3 — 重东西放外面，用到再拿

大段的参考文档、API 说明、模板代码不要全塞在 SKILL.md 里。拆成独立文件放在 `references/` 或 `templates/` 目录下，AI 用到时才去读。

```
❌ SKILL.md 里塞了 200 行 API 文档 → 每次触发都加载
✅ 200 行放到 references/api.md → 只有 AI 需要查 API 时才读
```

可执行的确定性任务（比如数据处理脚本）放 `scripts/`，执行时不加载到上下文，只拿运行结果。

---

### 技巧 4 — 关键步骤设卡，不过卡不能走

复杂流程里，有些步骤绝对不能跳过。在 Skill 里给这些步骤加"闸门"，AI 想绕都绕不过去。

三种闸门：
- **硬刹车** `<HARD-GATE>` 标签——到这必须停，确认了才能继续
- **防死循环** `≥3 次失败修复 = 质疑整个方案`——防止 AI 在同一个坑里反复转圈
- **强制检查** Gate 函数——比如"提供终端截图证明测试通过了"，没截图就不算过

---

### 技巧 5 — 每种情况都说清楚怎么办

AI 最怕的不是复杂的流程，是走到一半遇到情况 Skill 里没写。把可能出现的状态全列出来，每种都给下一步动作。

```markdown
| 子任务状态 | 下一步 |
|-----------|--------|
| 做完了 | 进入 spec review |
| 做完了但有点担心 | 评估风险，决定是否重做 |
| 缺信息做不下去 | 收集缺失信息，重新派活 |
| 卡住了 | 分析原因，换方案还是换人 |
```

只写 happy path 的 Skill = 一遇到意外就崩。

---

### 技巧 6 — 对的和错的摆在一起，AI 秒懂

AI 对"对比模式"极其敏感。不要只描述正确的做法，把错误的也写出来放在旁边。

```markdown
✅ 先写测试 → 跑红 → 写代码 → 跑绿 → 重构
❌ 先写代码 → 跑通了 → "回头补个测试"
```

用 `✅` / `❌` 或用 `<Good>` / `<Bad>` 标签都可以，关键是二者必须紧挨着。

---

### 技巧 7 — 派活给子 Agent 时只给最少信息

当你把任务分给子 Agent 时，不要把你的全部上下文都扔过去。只给执行这个任务必须知道的最小信息量。

原因很简单：给的信息越多，子 Agent 越容易被不相干的内容带偏。调用方负责裁剪上下文，执行方只拿刚好够用的。

---

### 技巧 8 — 先跑一遍看 AI 怎么犯错，再针对性地写

不要凭想象写 Skill。标准流程：

```
第 1 轮：不写 Skill，直接让 AI 跑压力场景
        → 记录 AI 的每一个错误、每一个取巧话术

第 2 轮：针对第 1 轮发现的问题，写最小版本的 Skill
        → 再跑同一场景，看问题修没修掉

第 3 轮：第 2 轮中又发现了新的取巧方式
        → 在 Skill 里显式封堵，重测至坚不可摧
```

一句话：**没有 baseline 数据的 Skill = 没有单元测试的代码。** 你根本不知道它有没有用。

---

### 技巧 9 — 硬性绑定预制组件，禁止 Agent 手写替代品 🎯

Agent 有一种危险倾向：看到预制组件（assets/ 下的 .jsx / .html），仍然选择"我自己写一个"。但自己写的 99% 会有位置 bug、规格偏差、或遗漏细节。

huashu-design 的做法：对容易出错的组件（iOS 设备框、动画引擎、幻灯片 Stage）不仅提供预制文件，还用**硬性规则**禁止手写：

```markdown
## iOS 设备框必须用 `assets/ios_frame.jsx`——禁止手写 Dynamic Island / status bar

**禁止在你的 HTML 里自己写**以下任何一项：
- `.dynamic-island` / `.island`
- `.status-bar` with 手写的 时间/信号/电池图标
- `.home-indicator` / 底部 home bar

自己写 99% 会撞位置 bug——iPhone 15 Pro 的刘海是固定 124×36 像素，
留给 status bar 两侧的可用宽度很窄，不是你凭空估的。
```

写法要素：
- 明确列出**禁止手写的具体内容**（CSS class 名、DOM 结构）
- 给出**为什么自己写会出错**的技术原因
- 提供**正确用法**的代码示例
- 写明**例外条件**（什么情况下可以绕过）

---

### 技巧 10 — 建立领域资产优先级，告诉 Agent 什么比什么更重要 🎯

Agent 面对多个子任务时默认平均用力。但不同领域有天然的优先级——告诉 Agent 什么 > 什么，它才能在 token/时间有限时做正确的 trade-off。

huashu-design 的核心资产优先级金字塔：

```markdown
| 资产类型 | 识别度贡献 | 必需性 |
|---------|----------|-------|
| Logo | 最高 | 任何品牌都必须有 |
| 产品图/UI截图 | 极高 | 实体/数字产品必须有 |
| 色值 | 中（辅助） | 辅助 |
| 字体 | 低（辅助） | 辅助 |
```

这直接推翻了传统设计教学的"色值+字体优先"——实测中 logo 和产品图的识别度贡献远超色值。

写法模板：

```markdown
## 优先级金字塔（从高到低）
1. X — 没有 X 整个产出物失去意义（必须）
2. Y — 没有 Y 质量打对折（强烈推荐）
3. Z — 锦上添花（有最好，没有也能交付）

**铁律**：找不到 X 就停下，不要用 Y 和 Z 凑。
```

**关键**：优先级必须来自实测数据或领域经验，不能凭感觉排序。同时给出"找不到最高优先级时怎么办"的兜底策略（而不是让 Agent 自己发挥）。

---

### 技巧 11 — Reference 路由表加优先级标注，Agent 不会漏读关键文件 🔀🧭

当 Skill 的 references/ 超过 5 个时，Agent 面临选择困难：全读太贵，随机读可能漏掉关键文件。平铺的"任务→文件"路由表不够——Agent 不知道哪个是"不读必翻车"的。

加优先级标注：

```markdown
| 优先级 | 任务 | 读 | 不读的后果 |
|--------|------|-----|-----------|
| 🔴 必读 | 做动画 | animation-pitfalls.md | 必返工 1-3 轮 |
| 🔴 必读 | 涉及具体品牌 | 走核心资产协议（§1.a） | 品牌识别度归零 |
| 🟡 推荐 | 做幻灯片 | slide-decks.md | CSS 特异性踩坑 |
| 🟢 按需 | 导出可编辑 PPTX | editable-pptx.md | — |
```

**关键**：必须标注"不读的后果"——这是 Agent 做 ROI 判断的依据。只标"必读"不说为什么，Agent 还是会 skip。

---

## 八、SKILL.md 正文推荐结构（新增字段）

```markdown
---
name: skill-name           # 仅字母、数字、连字符；动名词优先
description: Use when [触发条件]  # 第三人称，只写"何时用"，绝不泄露工作流
---

# Skill Name

## Overview
1-2 句话，核心原则

## When to Use
症状/触发场景 + 何时 NOT 用 + [流程图仅非显然决策用]

## Core Pattern（技术/模式类）
Before/After 代码对比

## Quick Reference
表格或 bullet，快速扫描

## Implementation
简单模式：内联代码（< 50 行）；重型参考：链接到独立文件

## Common Mistakes
常见错误 + 修复

## Red Flags（纪律类必加）
"如果你在想 X → STOP"

## Rationalization Table（纪律类必加）
| 借口 | 现实 |

## Integration（编排类必加）
Required skills + Called by + Pairs with

## Real-World Impact（推荐）
具体数据（参考套路 13：规则附真实失败故事+成本对比）

## Asset Priority Pyramid（领域知识类推荐）
告诉 Agent 这个领域什么 > 什么（参考技巧 10）

## Hard-Bound Components（有 assets/ 时推荐）
列出禁止手写的组件清单（参考技巧 9）
```

---

## 九、反模式（Anti-Patterns）

| 反模式 | 为什么坏 | 正确做法 |
|--------|---------|---------|
| description 泄露工作流 | AI 走捷径跳过正文 | 只写触发条件 |
| 无 baseline 测试就发布 | 你以为有效，实际只是你以为 | TDD: RED → GREEN → REFACTOR |
| 多语言示例 | 平庸质量、维护负担 | 一个优秀示例，一种语言 |
| `@` 强制加载引用文件 | 烧 200K+ 上下文 | 语义引用：`**REQUIRED BACKGROUND:** xxx` |
| 流程图用于线性步骤 | 比编号列表更难读 | 线性步骤用编号列表 |
| 没有 When NOT to Use | Skill 被滥用到不该用的场景 | 显式写边界 |
| 借口表凭空编造 | 不是 Agent 真正会说的话 | 从 baseline 压力测试提取 |
| 纪律类 Skill 没有 Red Flags | Agent 不会自查 | 加自查清单，用 Agent 的内心独白做触发词 |
| 打包依赖时连 config/凭证一起复制 | 分享后别人用你的身份跑数据，且你无感知 | rsync 排除 config*.yaml/.env/*credential*/*secret*，或复制后清空凭证字段并加注释"使用前填入自己的凭证" |

---

## 十、好 Skill 的「产品-技术」平衡点

```
产品好 ≠ 写得长、覆盖全
技术好 ≠ token 少、拆得细

真正的产品好 = Agent 用了之后：
  结果对（需求满足）→ 过程顺（体验好）→ 复杂流程能走到底（引导充分）

真正的平衡点：
  每一行字都在为「让下次触发的 Agent 少犯一种错」服务
  每一个文件拆分都在为「不被触发时不付费」服务
  每一个借口对照行都来自真实的 baseline 失败数据
  每一个闸门和检查点都在防止 Agent 在复杂流程中迷失或跳步
```

> **好 Skill = 对抗性设计 × token 预算 × 数据驱动迭代**。三者缺一不可。
>
> **评价好 Skill = 看执行结果（🎯）× 看执行体验（🧭）× 看流程完整性（🔀）**。三者加权因 Skill 类型而异。

---

## 十一、Skill 创建检查清单

### RED Phase — 写失败测试
- [ ] 创建压力场景（纪律类 3+ 组合压力）
- [ ] 不加 Skill 跑场景，记录 baseline 行为（逐字记录取巧话术）
- [ ] 识别话术模式

### GREEN Phase — 写最小 Skill
- [ ] name 仅用字母、数字、连字符
- [ ] description 以 "Use when..." 开头，只写触发条件
- [ ] 第三人称
- [ ] 关键词散布全文
- [ ] 清晰的 Overview
- [ ] 针对 baseline 发现的具体问题
- [ ] 一个优秀示例（非多语言）
- [ ] 加 Skill 跑场景，验证通过

### REFACTOR Phase — 封堵漏洞
- [ ] 识别测试中发现的新取巧话术
- [ ] 显式封堵（纪律类）
- [ ] 构建借口-现实对照表
- [ ] 创建 Red Flags 清单
- [ ] 重测至坚不可摧

### Quality Checks（技术维度）
- [ ] 流程图仅用于非显然决策
- [ ] Quick Reference 速查表
- [ ] Common Mistakes 章节
- [ ] 无叙事性故事
- [ ] 支持文件仅用于工具或重型参考
- [ ] Reference 路由表有优先级标注（参考技巧 11）
- [ ] 预制组件有硬性绑定规则，禁止 Agent 手写（参考技巧 9）
- [ ] 领域资产有优先级金字塔（参考技巧 10）
- [ ] 凭证安全：打包/分享 Skill 前确认 dependencies/ 中不含 appkey、secret、token、emp_id 等个人凭证（真实踩坑：rsync 复制 config.yaml 导致他人用你身份跑数据）

### Quality Checks（产品维度）
- [ ] 🎯 需求满足：baseline vs 加 Skill 的结果有无显著差异？
- [ ] 🎯 输出质量：不只是"做完了"，产出物符合用户的质量预期吗？
- [ ] 🧭 认知负荷：Agent 5 秒内能找到关键指令吗？
- [ ] 🧭 纠错成本：Agent 走偏了，Skill 有机制拉回来吗？
- [ ] 🔀 步骤可感知：多步骤流程中，Agent 始终知道自己在第几步吗？
- [ ] 🔀 异常路径：不只是 happy path，出错/卡住/不确定时怎么说？
- [ ] 🔀 完成标准："做完了"由外部清单判定，不由 Agent 主观判断
- [ ] 🎯 规则说服力：关键禁令是否附上了逻辑链论证（参考套路 11）？
- [ ] 🎯 规则说服力：关键规则是否附上了真实失败故事+成本对比（参考套路 13）？
- [ ] 🧭🔀 需求模糊处理：是否有 Fallback 模式用预置知识库给选项而非追问（参考套路 12）？
